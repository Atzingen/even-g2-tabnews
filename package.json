{
  "name": "even-g2-tabnews",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vitest run && vite build",
    "preview": "vite preview",
    "pack": "npm run build && evenhub pack app.json dist --sdk-ver 0.0.14",
    "test": "vitest run",
    "simulate": "GDK_BACKEND=x11 WEBKIT_DISABLE_DMABUF_RENDERER=1 evenhub-simulator http://localhost:5173",
    "simulate:auto": "GDK_BACKEND=x11 WEBKIT_DISABLE_DMABUF_RENDERER=1 evenhub-simulator --automation-port 9898 http://localhost:5173"
  },
  "dependencies": {
    "@evenrealities/even_hub_sdk": "0.0.14",
    "@evenrealities/pretext": "0.1.4"
  },
  "devDependencies": {
    "@evenrealities/evenhub-cli": "^0.1.14",
    "@evenrealities/evenhub-simulator": "^0.9.5",
    "typescript": "^5.7.0",
    "vite": "^5.4.0",
    "vitest": "2.1.9"
  }
}
